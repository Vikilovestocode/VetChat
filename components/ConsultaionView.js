import * as React from 'react';

const ConsultView = (props) => {

    return (
        <ScrollView style={{ flex: 1 }}>
            <View >
                <View >
                </View>
            </View>
        </ScrollView>    
    )

}

const styles = StyleSheet.create({
    padding: { padding: 5 },
})

export default ConsultView;